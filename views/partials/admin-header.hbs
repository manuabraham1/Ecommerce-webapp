<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Admin Dashboard</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02"
                aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
                {{#if adminSession}}
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="view-products">All Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="view-orders">All Orders</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="view-users">All Users</a>
                    </li>
                </ul>
                {{else}}
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                </ul>
                {{/if}}
                <div class="dropdown">
                    <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown"
                        aria-expanded="false"> 
                        {{#if adminSession}}
                            Admin
                        {{else}}
                            Admin Account 
                        {{/if}}
                        </button>
                    <ul class="dropdown-menu">
                        <li>
                        {{#if adminSession}}
                            <a class="dropdown-item" href="logout">Logout</a>
                        {{else}}
                            <a class="dropdown-item" href="admin">Login</a>
                        {{/if}}
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>
</header>